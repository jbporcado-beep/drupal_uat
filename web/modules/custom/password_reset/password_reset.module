<?php

/**
 * Implements hook_route_alter().
 */
function password_reset_route_alter(&$routes) {
  if ($route = $routes->get('user.reset')) {
    $route->setDefault('_form', \Drupal\password_reset\Form\SetPasswordForm::class);
  }
}
